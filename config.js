// Frontend runtime configuration.
// Production API URL can be supplied here after the Render service is created.
// Keeping the fallback local makes local development work without a deployed API.
window.CHILLIPROFIT_API_URL = window.CHILLIPROFIT_API_URL || 'http://127.0.0.1:8000';
